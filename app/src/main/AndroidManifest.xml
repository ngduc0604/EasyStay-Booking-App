<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HotelBookingApp">
        <activity
            android:name=".activities.ReviewHotelActivity"
            android:exported="false" />

        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/client_token" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.UserHomeActivity" />
        <activity android:name=".activities.HotelListActivity" />
        <activity android:name=".activities.HotelDetailActivity" />
        <activity android:name=".activities.AccountActivity" />
        <activity android:name=".activities.ConfirmAccountActivity" />
        <activity android:name=".activities.BookingActivity" />
        <activity android:name=".activities.AccountUpdateActivity" />
        <activity android:name=".activities.SavedHotelsActivity" />
        <activity android:name=".activities.BookingHistoryActivity" />
        <activity android:name=".activities.BookingDetailActivity" />
        <activity android:name=".activities.ReviewsActivity" />
    </application>

</manifest>